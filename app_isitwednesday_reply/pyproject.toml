[project]
name = "app-isitwednesday-reply"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "core",
    "clients",
]

[tool.uv.sources]
core = { workspace = true }
clients = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/app_isitwednesday_reply"]
